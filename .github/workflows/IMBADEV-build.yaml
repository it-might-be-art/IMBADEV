# To better understand this file read this:
# https://docs.ionos.space/docs/github-actions-customization/

name: "Deploy Now: Execute Build"

on:
  workflow_call:
    inputs:
      site-url:
        required: true
        type: string
      branch-id:
        required: true
        type: string

env:
  DEPLOYMENT_FOLDER: /it-might-be-art

jobs:
# Please do not rename this job
  build-project:
    name: build IMBADEV
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v4
        with:
          submodules: 'recursive'

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: v22.x

      - name: Ensure deployment folder exists
        run: mkdir -p ${{ env.DEPLOYMENT_FOLDER }}

      - name: List deployment folder contents for debugging
        run: ls -alh ${{ env.DEPLOYMENT_FOLDER }}

      - name: Install dependencies
        run: npm install

      - name: Run tests
        run: npm test

      - name: Build project
        run: npm run build

      - name: List project root directory contents for debugging
        run: ls -alh ${{ github.workspace }}

      - name: List root directory contents for debugging
        run: ls -alh /

      - name: List deployment folder contents for debugging after build
        run: ls -alh ${{ env.DEPLOYMENT_FOLDER }}
        
# Please do not touch the following action
      - name: Store deployment content
        uses: ionos-deploy-now/artifact-action@v1
        with:
          api-key: ${{ secrets.IONOS_API_KEY }}
          service-host: api-eu.ionos.space
          project-id: f0e53aff-5376-4069-ab7a-dda3532eeaef
          branch-id: ${{ inputs.branch-id }}
          version: ${{ github.sha }}
          folder: ${{ env.DEPLOYMENT_FOLDER }}
          config-file: .deploy-now/IMBADEV/config.yaml
          action: upload
